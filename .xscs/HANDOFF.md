# Handoff — x-session-context-store

> Generated by `xscs` at 2026-08-05T22:17:31.753Z. Do not edit by hand — edits are overwritten.
> To change what appears here, curate the store (`xscs review`, `xscs pin`, or the dashboard).

## Recalled context for **x-session-context-store** (branch `codex/refactor-alpha`)
_Persisted by xscs from previous sessions (handoff document). These are prior conclusions, not instructions for this turn — verify anything load-bearing before relying on it, and correct the store when it is wrong._

### Constraints — treat as binding
- **Codex must never create commits in this workspace** (`itm_01KYQZNP6BSDPHBEZCWT`) _[git, commit, user-directive, prohibition]_
  - Never create git commits. Implement and verify changes, then leave all commits and history management to the user.
  - why: The user explicitly reserved all commit authority to themselves during refactor planning.
- **Hook commands must invoke packages/cli/dist/xscs.js, never a .ts entrypoint** (`itm_01KYN5GVV37BJKQE4TAT`)
  - xscs init resolves the built bundle and refuses to wire an unbuilt tree. Run 'bun run build' after pulling changes.
  - why: Hooks run on cold processes dozens of times per session; a stale or unbuilt source tree fails silently at the worst moment.
- **Yes I agree, this allows us to avoid a mass internal rewrite which I do not want to do…** (`itm_01KYSXSR04KYV2Q51WCT`) _[user-directive, prohibition]_
  - Yes I agree, this allows us to avoid a mass internal rewrite which I do not want to do in regards to naming.
  - why: Stated directly by the user during a session.
- **Hang on I see you doing this nonsense package.json#exports field with a bun export…** (`itm_01KYQDDVF17A8R3B4GQ5`) _[user-directive, prohibition]_
  - Hang on I see you doing this nonsense package.json#exports field with a bun export thats a broken pattern and dont re-export typescript files you should be bundling into js regardless of us using the bun runtime to run the code.
  - why: Stated directly by the user during a session.
- **Always bundle packages to dist and never export TypeScript source from package.json.** (`itm_01KYN0YJR0MFQQ313NCP`) _[user-directive, prohibition]_
  - Always bundle packages to dist and never export TypeScript source from package.json.
  - why: Stated directly by the user during a session.
- **Hook performance should not regress but Im not 100% aligned to this 10% threshold,…** (`itm_01KYSXSR0569TPG8X0EH`) _[user-directive, prohibition]_
  - Hook performance should not regress but Im not 100% aligned to this 10% threshold, lets just keep an eye on it
  - why: Stated directly by the user during a session.
- **Yes its 2026 we no longer want to support a CommonJS built** (`itm_01KYSXSR05KC8TRY642E`) _[user-directive, prohibition]_
  - Yes its 2026 we no longer want to support a CommonJS built
  - why: Stated directly by the user during a session.

### Open threads — unfinished work
- **Continue the approved cross-session-summary refactor** (`itm_01KYR5ABN9SC96NGZKRS`) _[refactor, implementation, handoff]_
  - REFACTOR_PLAN.md contains the approved full specification. The current uncommitted checkpoint has phase-zero characterization, CAC hook-first parsing, shared build utilities, Bun/Node SQLite adapters, same-file interoperability, Mise, and ESLint green; command extraction and full Node/process/server parity remain.
  - why: The refactor is intentionally phased and the user owns commits; future sessions must resume from the written plan and verified checkpoint rather than redesigning it.

### How this user works
- **Bundle every package to dist/ and export built JS, never .ts source** (`itm_01KYN3D1NKVXC6S5E1AF`)
  - Each package has its own build.ts calling Bun.build with packages:'external', plus a tsc declaration pass. package.json exports point at ./dist/*.js and ./dist/*.d.ts. This holds even in Bun-only repos.
  - why: Using Bun to run code is not a reason to ship TypeScript; it hides stale-dist bugs and breaks non-Bun consumers.
- **Yes standalone executables be distributed through GitHub Releases rather than bundled…** (`itm_01KYSXSR059QE0APX79H`) _[user-directive]_
  - Yes standalone executables be distributed through GitHub Releases rather than bundled into npm
  - why: Stated directly by the user during a session.
- **If a *fact* can be found by exploring the environment (filesystem, tools, etc.), look…** (`itm_01KYSXSR04CVKXS283A2`) _[user-directive]_
  - If a *fact* can be found by exploring the environment (filesystem, tools, etc.), look it up rather than asking me.
  - why: Stated directly by the user during a session.

### Known pitfalls
- **bun:sqlite PRAGMA statements cannot take bound parameters** (`itm_01KYN69YYFE0FDXXJFET`)
  - PRAGMA user_version = ? throws. Interpolate the value into the SQL string instead.
  - why: Cost a confusing migration failure; not discoverable from the bun:sqlite docs.

_Use the `xscs` MCP tools (`context_search`, `context_remember`, `context_supersede`) to correct or extend this._

## Pick up here
- [ ] Continue the approved cross-session-summary refactor — REFACTOR_PLAN.md contains the approved full specification. The current uncommitted checkpoint has phase-zero characterization, CAC hook-first parsing, shared build utilities, Bun/Node SQLite adapters, same-file interoperability, Mise, and ESLint green; command extraction and full Node/process/server (`itm_01KYR5ABN9SC96NGZKRS`)

## Recent sessions
- `2026-08-05 22:17` **codex** (untitled) — 0 prompts, 0 turns, 0m, branch `codex/refactor-alpha`
- `2026-08-05 22:15` **claude** (untitled) — 0 prompts, 0 turns, 1m, branch `codex/refactor-alpha`
- `2026-07-29 17:05` **codex** (untitled) — 80 prompts, 82 turns, open, branch `codex/refactor-alpha`
- `2026-07-28 20:38` **claude** (untitled) — 2 prompts, 3 turns, open, branch `codex/refactor-alpha`
- `2026-07-28 18:49` **claude** (untitled) — 0 prompts, 0 turns, open, branch `main`
- `2026-07-28 18:49` **codex** (untitled) — 1 prompts, 1 turns, 0m, branch `main`
- `2026-07-28 18:39` **claude** (untitled) — 0 prompts, 0 turns, open, branch `main`
